# Supabase project (Project Settings → API). Without these values the app
# runs in demo mode on sample data.
VITE_SUPABASE_URL=http://127.0.0.1:55321
VITE_SUPABASE_ANON_KEY=your-anon-key
# Optional: x selects OAuth 2.0. Default: twitter (legacy OAuth 1.0a).
VITE_X_AUTH_PROVIDER=twitter
